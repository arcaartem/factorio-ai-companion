---------------------------------------------------------------------------------------------------
Version: 0.17.0
Date: 2026-07-26
  Bugfixes:
    - resource_nearest and world_nearest now actually return the NEAREST match. Both scanned a
      +/-200 square with limit=100 and then picked the minimum from whatever came back, but
      find_entities_filtered's limit truncates in the engine's chunk order with no distance
      ordering - so the true nearest could be excluded from the sample outright. Probed live
      returning iron-ore at distance 155 while iron-ore sat at 85.5. Both now grow an unlimited
      circular search (8 -> 16 -> 32 -> 64 -> 128 -> 200), which is complete by construction:
      whatever the first non-empty circle contains, everything omitted is farther than its
      radius. The near case stays cheap; only genuinely empty surroundings pay for a wide scan
    - resource_nearest no longer floors the coordinates it returns. Ore sits at tile centres
      (x.5, y.5), so flooring reported a point ~0.71 tiles off the actual entity - spent
      straight out of the engine's 2.7-tile resource_reach_distance before the caller had
      walked anywhere. Reported distance is likewise no longer floored, which had it
      under-report and skip a needed walk
  Changes:
    - world_nearest's water branch searches the same expanding circle as the entity branches
      instead of a limited square, and reports an unfloored distance

---------------------------------------------------------------------------------------------------
Version: 0.16.0
Date: 2026-07-26
  Bugfixes:
    - Fixed the harvest queue pinning the companion in place forever: the engine reverts
      walking_state every tick while mining_state.mining is true, so a walk/combat queue
      trying to move the companion could never actually move it. The harvest tick now yields
      to an actively-moving walk/combat queue instead of fighting it, and re-asserts mining
      once movement stops
    - Fixed harvested count staying at 0 forever: mining_state.mining stays true for as long
      as the ore tile has ore left (it's a per-amount decrement, not a per-ore event), so the
      old "mining stopped -> count" guard never ran. Inventory deltas are now counted every
      tick instead
    - Added a 15s stall-guard timeout to the harvest queue so it can never hang indefinitely
      even if the above two paths are both blocked
    - start_mining_next now skips pool entities beyond resource_reach_distance - the pool is
      seeded from a wider radius-3 search than the engine's actual mining reach (2.7)
    - companion_stop_all now also clears mining_state, not just the queue and walking_state
    - resource_mine_status now reports a finished queue's harvested/target/reason instead of a
      bare {active=false}. The queue is deleted on the same tick its final ore is counted, so
      the terminal poll previously had no way to read the outcome at all - which made
      mine-until's success path return 0 even once counting itself was fixed
    - fac_resource_mine now propagates start_harvest's error instead of always reporting
      mining:true/status:"started"
  Changes:
    - mine-until's walkTo now also treats the walk queue reporting active:false as arrival,
      not just a hard-coded distance check - a companion could land between the two
      thresholds and spin for the full walk timeout with no queue behind it
    - mine-until's mining timeout path now uses the harvested count from the stop response
      instead of the last (potentially stale) status poll
---------------------------------------------------------------------------------------------------
Version: 0.15.0
Date: 2026-07-26
  Changes:
    - Reach enforcement is now always on for every companion, matching human player limits -
      removed the opt-in companion_realistic flag (and the fac_companion_realistic command)
      entirely, per the player-parity decision superseding the earlier opt-in design
    - The harvest queue's too-far abort now shares check_reach's single reach-limit source
      instead of hand-rolling its own distance check with a different fallback value
---------------------------------------------------------------------------------------------------
Version: 0.14.0
Date: 2026-07-26
  Features:
    - Companions are now armed on spawn by taking a gun and matching ammo from the player's
      main inventory, if any is available - never conjured from nothing. An unarmed spawn
      is a valid outcome (spawn response now reports armed/weapon/ammo/ammo_count/arm_reason)
    - start_combat auto-equips from the companion's own inventory first (e.g. items picked
      up via item_pick/building_empty), and now hard-fails with "No weapon equipped" or
      "No ammo" instead of shooting with nothing
  Changes:
    - Combat kills are now credited by attribution (on_entity_died's event.cause) instead
      of inferring a kill whenever the current target slot went invalid - the old inference
      over-counted (any death reason) and dropped every bystander kill in a cluster.
      get_combat_status/stop_combat now also report uncaused_deaths, a diagnostic-only
      counter for enemy deaths not attributed to the companion
    - A combat round's kill count now survives the companion dying mid-fight or being
      stopped via companion_stop_all, instead of being discarded with the queue
    - companion_disappear and /fac kill now spill the companion's gun and ammo (not just
      main inventory) onto the ground instead of destroying them - companions carry the
      player's real items now, not conjured freebies
---------------------------------------------------------------------------------------------------
Version: 0.13.4
Date: 2026-07-23
  Bugfixes:
    - Fixed inventory handling for Factorio 2.0's get_contents() array-of-records shape (harvest tracking, companion_disappear item spill, container/character inventory inspect) - previously misread as a name->count map
    - Fixed error_response emitting hand-built JSON that broke on special characters; now uses the same safe table_to_json encoder as normal responses
    - Reach enforcement (opt-in via companion_realistic) now applies to harvest and build queues, matching move/attack/pickup parity; harvest reach aborts are logged instead of silent
    - Combat queue now counts and reports kills (get_combat_status.kills)
---------------------------------------------------------------------------------------------------
Version: 0.13.3
Date: 2025-01-17
  Features:
    - Auto-stop skills before movement/action commands (move_to, move_follow, attack, flee, patrol, wololo)
---------------------------------------------------------------------------------------------------
Version: 0.13.2
Date: 2025-01-17
  Bugfixes:
    - Fixed max_health access for Factorio 2.x API (use entity.max_health directly)
---------------------------------------------------------------------------------------------------
Version: 0.13.1
Date: 2025-01-17
  Features:
    - Added fac_companion_stop_all command to clear all queues
    - Added combat_until skill for autonomous hunting
  Changes:
    - Combat skill handles scan, move, attack cycle
    - Companion stop_all clears harvest, craft, build, combat, walk queues
---------------------------------------------------------------------------------------------------
Version: 0.13.0
Date: 2025-01-17
  Features:
    - Added centralized configuration system
    - Added safe_command wrapper to reduce boilerplate
  Changes:
    - All messages translated to English
    - Improved code organization and DRY principles
  Bugfixes:
    - Fixed connection retry logic duplication
---------------------------------------------------------------------------------------------------
Version: 0.12.3
Date: 2025-01-16
  Features:
    - Hybrid mining system with native Factorio API + auto-restart
    - Reduced wololo range from 200 to 25 tiles for balance
---------------------------------------------------------------------------------------------------
Version: 0.12.0
Date: 2025-01-15
  Features:
    - Added companion map markers
    - Added 51 RCON commands for AI control
    - Added autonomous mining skill
---------------------------------------------------------------------------------------------------
Version: 0.1.0
Date: 2025-01-10
  Features:
    - Initial release
    - Basic companion spawning and control
    - RCON integration
